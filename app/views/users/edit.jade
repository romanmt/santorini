.container
  .conthead
    h1 Edit User
  .contentbox
    form#save(method: "post")
      .inputboxes
        !=text_tag_for("user", "name", user.name);
      .inputboxes 
        !=text_tag_for("user", "email", user.email);
      .inputboxes
        !=integer_tag_for("user", "acrualRate", user.acrualRate);
      .buttons 
        input(type="submit", value="Save").btn 
        span &nbsp; or &nbsp;
        a(href: '/') cancel
